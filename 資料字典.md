# 資料字典

| 資料表 | 用途 | 主鍵／重要關聯 |
|---|---|---|
| `profiles` | 使用者、顯示名稱、教練／主管角色 | `id` 對應 Supabase Auth user |
| `members` | 會員主檔 | `created_by` → profiles |
| `daily_operations` | 每日營運指標 | 日期 + 教練唯一；`coach_id` → profiles |
| `purchases` | 課程購買主檔 | `member_id` → members；`coach_id` → profiles |
| `purchase_payments` | 各期實際付款 | `purchase_id` → purchases；同購買期次唯一 |
| `session_usages` | 每次銷課與金額 | `purchase_id` → purchases；同購買堂次序號唯一 |
| `purchase_balances` | 即時計算使用／剩餘堂數及金額 | 資料庫 View，不另存重複餘額 |

## 主要欄位定義

- `classes_held`：實際完成上課堂數。
- `classes_cancelled`：排定但取消堂數；Dashboard 取消率分母為完成堂數 + 取消堂數。
- `trial_conversions`：體驗後成交人次，不得大於同筆體驗人次。
- `total_amount`：該次購買的成交總額；幣別由營運政策統一，本版畫面以 `$` 符號顯示。
- `installment_count`：購買約定總期數，1–3 期。
- `deducted_amount`：該堂認列的扣課金額；非會計收入認列憑證。

## v1.1.0 專案資料

| 資料表／檢視 | 用途 |
|---|---|
| `projects` | 非會員專案主檔、已儲值／未儲值類型及儲值金額 |
| `project_catalog` | 每個專案的操作項目、時數與價格 |
| `project_entries` | 專案每日執行明細、使用者、教練、數量、金額與備註 |
| `project_funding_balances` | 已儲值專案的儲值、已使用及剩餘金額 |
| `project_deposits` | 專案每次儲值、期初儲值及沖銷歷史 |

- `funding_type`：`stored` 為已儲值；`unfunded` 為未儲值／事後請款。
- `stored_date`：實際收到專案儲值款的日期；未儲值專案為空白。
- `stored_amount`：專案實際儲值總金額，不得低於已使用金額。
- `project_deposits.amount`：正數為儲值；負數為沖銷。累計加總應與專案儲值總額一致。

## v1.4.0 每月報表資料

- `course_catalog.course_type`：畫面顯示名稱為「課程屬性」，底層資料庫欄位名稱不變。
- 每月報表使用開始日期與結束日期（含首尾兩日）篩選所有明細與匯出資料；預設為本月月初至月底。
- `談單獎金率`：3%；成交日期在查詢期間內，成交含稅總金額除以 1.05 換算未稅後計算，歸屬購買紀錄的指導教練。
- `結單獎金率`：4%；最後一堂銷課日期在查詢期間內，以該課程原成交含稅總金額除以 1.05 換算未稅後計算，歸屬最後一堂授課教練。
- 獎金率集中定義於程式常數 `TALK_BONUS_RATE` 與 `COMPLETION_BONUS_RATE`，獎金結果四捨五入為整數。
- 期限查詢以 `purchase_balances.expiry_date` 作為日期區間條件；剩餘天數為有效期限減查詢當日，負數代表已過期。
- 課程完成／課程結束以 `remaining_sessions <= 0` 判定。
- `課程完成日期`：課程完成時，取該購買課程最後一筆 `session_usages.usage_date`；未完成課程為空白。
- `即將到期`：有效期限介於查詢當日至未來 30 天（含首尾）；`過期`：有效期限早於查詢當日。

## v1.8.0 獎金規則

- `bonus_rules`：保存每一期獎金制度，不覆蓋歷史比例；啟用規則的日期區間不得重疊。
- `talk_rate`、`completion_rate`：以小數儲存，例如 3% 儲存為 `0.03`。
- `referral_first_talk_eligible`、`referral_first_completion_eligible`：醫生轉介首購是否計算談單／結單獎金。
- `referral_renewal_talk_eligible`、`referral_renewal_completion_eligible`：醫生轉介續約是否計算談單／結單獎金。
- 談單獎金依購買日期尋找適用規則；結單獎金依最後一堂銷課日期尋找適用規則。
- 獎金計算基礎為成交含稅總金額除以 1.05 並四捨五入成未稅整數，再乘規則比例，獎金結果四捨五入為整數。
- 各教練獎金總計僅彙總 `計算狀態=已計算` 的明細；規則排除或查無規則的紀錄不納入計算基礎與獎金總計。

- `operation_item_catalog.default_amount`：體驗項目或單堂銷售的預設未稅金額。
- `trial_items.amount`：體驗項目建立當下的未稅金額快照。
- `single_sales.amount`：單堂銷售建立當下的未稅金額快照。
- 每日營運的體驗項目與單堂銷售畫面以「金額」呈現，底層仍分別儲存在上述 `amount` 欄位。
- 專案及銷課原始金額視為含稅，月報未稅金額按含稅金額除以 1.05 後四捨五入至整數。

## v1.4.3 課程狀態

- `course_catalog.active`：`true` 為啟用，可供新購買選擇；`false` 為停用，僅保留歷史資料。

## v1.5.0 體驗項目內容

- `operation_item_catalog.item_name`：體驗項目名稱。
- `operation_item_catalog.detail_content`：該體驗項目預設帶入的內容。
- `trial_items.content`：建立紀錄當下的體驗項目快照。
- `trial_items.detail_content`：建立紀錄當下的內容快照。
- `line_amount`：每筆專案操作的實際金額快照，避免單價除算造成差額。
## v1.5.1 體驗項目唯一性

- 體驗項目以 `item_type + item_name + detail_content` 判斷是否重複。
- 同一個體驗項目名稱可以建立多個不同內容。
- 非體驗類型仍以 `item_type + item_name` 判斷是否重複。
## v1.6.0 體驗項目狀態與課程購買修改

- `operation_item_catalog.active`：體驗項目是否可供新紀錄選用；停用不刪除歷史資料。
- 課程購買修改可更新 `purchases.purchase_kind` 與 `purchases.coach_id`。
- 購課紀錄搜尋顯示 `purchase_date`、會員名稱、課程名稱、`total_sessions` 與指導教練。
## v1.6.1 資料管理介面

- 課程名稱、體驗項目及單堂銷售的新增、刪除與修改功能分頁呈現。
- 修改課程目錄不回寫 `purchases` 的歷史成交快照。
- 修改營運項目目錄不回寫 `trial_items` 或 `single_sales` 的歷史紀錄。
## v1.6.2 專案操作項目管理

- 專案操作項目以新增、刪除及修改三個分頁呈現。
- 修改 `project_catalog` 的項目、時數或價格，不回寫 `project_entries` 的歷史單據快照。
## v1.6.3 會員財務報表欄位

- 「實際預收金額」：查詢期間內實際收到的未分期或分期款項。
- 「實際預收金額總計」：查詢結果中實際收款的合計。
- 欄位更名不改變原有含稅／未稅換算及篩選邏輯。
## v1.6.4 會員銷課總表

- `購買_ID`：銷課紀錄所屬的 `purchases.id` 完整識別碼。
- `堂數`：對應 `session_usages.session_seq`，表示該購買紀錄目前銷課至第幾堂；堂次序號不加總。
- `銷課金額`：依使用者選擇顯示含稅或未稅金額。
## v1.6.5 銷課堂次與課程狀態

- `堂數`：由 `session_usages.session_seq` 與 `purchases.total_sessions` 組成「目前堂次／購買總堂數」。
- `課程狀態`：目前堂次達購買總堂數時為「已完成」，否則為「進行中」。
- 課程狀態為報表計算欄位，不新增或覆寫資料庫欄位。
## v1.6.6 購買課程編號

- `購買_ID`：顯示購買課程編號，格式為 `YYYYMMDD-NNN`。
- `YYYYMMDD` 取自成交日期；`NNN` 為當日依建立時間及資料 ID 排序的三碼成交序號。
- Supabase 內部 UUID 僅用於資料關聯，不在會員銷課總表呈現。
## v1.7.0 執行時數

- `銷課時數`：每筆銷課對應的 `purchases.session_hours` 合計。
- `活動支援時數`：本報表依 `(event_supports.hours - event_supports.deducted_hours) ÷ 2` 計算。
- `每日營運時數合計`：體驗項目時數＋單堂銷售時數＋專案時數＋活動支援時數。
- `總執行時數`：銷課時數＋每日營運時數合計。
- 執行時數頁面日期篩選下方顯示：銷課時數、每日營運時數、總執行時數。
- 銷課時數明細表的最後一列為合計，彙總查詢條件內的銷課時數與銷課金額。
## v1.8.2 匯入銷課表 purchase_id 對照

- 銷課表的 `purchase_id` 優先使用系統匯出之購買課程編號，格式為成交日期加當日成交序號（例如 `20260817-001`）。
- 為相容舊版匯入檔，亦可填寫 `purchases.id` 的 UUID。
- 若同一份 Excel 同時匯入會員課程及銷課表，銷課表可使用會員課程工作表內非空白且未重複的 `purchase_id`，系統建立課程後會自動轉換為資料庫 UUID。
## v1.8.3 一般教練會員資料可見範圍

- `coach` 權限只可在營運、付款、銷課及教練查詢畫面看到 `coach_id = profiles.id` 的資料與會員名稱。
- `shared_coach`、`manager`、`admin` 維持跨教練查詢權限。
- 會員與教練的歸屬以課程購買資料 `purchases.coach_id`／`purchase_balances.coach_id` 判定；每日營運紀錄則以各資料表的 `coach_id` 判定。
## v1.8.4 銷課明細查詢

- 來源資料：`session_usages`，以 `purchase_id` 關聯 `purchases` 及 `purchase_balances`。
- 不限制日期區間，顯示所有符合教練與會員名稱條件的銷課紀錄，並依銷課日期由新至舊排序。
- `成交日`：`purchases.purchase_date`；`銷課日期`：`session_usages.usage_date`；每筆銷課紀錄的 `銷課堂數` 為 1。
- `剩餘堂數` 顯示查詢當下 `purchase_balances.remaining_sessions`，不是該次銷課完成當日的歷史餘額。
- 一般教練只載入 `purchase_balances.coach_id = profiles.id` 的課程資料。
## v1.8.5 銷課表匯入完整性檢核

- 匯入前以 `purchase_id` 彙總尚未存在的銷課筆數，並與 `purchases.total_sessions - session_usages 筆數` 核對。
- 既有紀錄比對鍵為 `purchase_id + usage_date + coach_id + note`，採筆數比對；相同鍵若資料庫有 N 筆，只略過上傳檔案中的前 N 筆。
- 匯入結果分為成功、已略過及失敗；失敗明細保留 Excel 原始列號以利修正與重試。
- 此比對用於避免同一匯入檔在部分成功後重送造成重複扣課；若業務上確實需要同日、同教練、同備註的新增銷課，應確認檔案筆數高於資料庫既有相同筆數。
## v1.8.6 一般教練填表日期限制

- `coach` 權限於 `trial_items.entry_date`、`single_sales.entry_date`、`purchases.purchase_date`、首期 `purchase_payments.paid_date` 及 `session_usages.usage_date`，只能輸入系統當日或未來日期。
- `shared_coach`、`manager`、`admin` 不受此限制，可依管理需求補登過去資料。
- 日期限制同時在 Streamlit 日期元件與表單送出邏輯執行。
## v1.8.7 分期付款 purchase_id

- 一筆分期課程只建立一筆 `purchases`；各期付款分別建立於 `purchase_payments`。
- `purchase_payments.purchase_id` 必須全部指向同一筆 `purchases.id`，因此第 1、2、3 期共用相同購買課程編號。
- 後續期款必須使用「課程購買 → 登錄後續期款」新增；不可為同一筆分期另建課程購買紀錄。
## v1.8.8 匯入重複資料明細

- 會員課程匯入重複判斷以非空白 `purchase_id` 為準，並區分「資料庫既有紀錄」與「本次檔案內重複」。
- 銷課表匯入重複判斷沿用 `purchase_id + usage_date + coach_id + note` 的多重集合比對方式，只略過資料庫中已存在的相同筆數。
- 匯入檢查通過後，畫面會顯示重複資料明細，並可下載 `匯入重複資料明細_YYYY-MM-DD.xlsx`。

## v1.8.9 課程購買管理識別碼

- 資料管理「修改／刪除」的課程購買選擇記錄顯示對外 purchase_id，格式為購買日期 `YYYYMMDD` 加當日成交序號。
- purchase_id 僅作為搜尋與辨識顯示；修改及刪除仍以資料庫 UUID 精確定位原始紀錄。

## v1.9.0 銷課管理查詢

- 資料管理「修改／刪除」的銷課表選擇記錄顯示對外 purchase_id；銷課紀錄本身仍以 `session_usages.id` 定位修改或刪除。
- `session_cancellations` 在資料管理介面統一顯示為「上課預約取消」，資料表名稱及既有資料不變。
- 資料匯入／匯出下的銷課查詢以 `session_usages.usage_date`、`session_usages.coach_id` 及會員名稱篩選。
- 查詢時數來自該銷課紀錄所屬 `purchases.session_hours`；舊資料若空白則以 1 計算。

## v1.9.1 銷課堂次唯一性修正

- `session_usages` 仍維持 `purchase_id + session_seq` 唯一限制。
- `consume_session` 在 `1..total_sessions` 範圍內選擇尚未使用的最小 `session_seq`，可安全補回被刪除的堂次空缺。
- 扣課金額仍以實際已銷課筆數判斷最後一堂；最後一筆可用堂數會扣除全部剩餘金額。
- 管理員刪除銷課紀錄時，若所屬課程狀態為 `completed`，會改回 `active`。

## v1.9.2 purchase_id 搜尋

- 資料管理「修改／刪除」的課程購買及銷課表可依 purchase_id 篩選。
- 搜尋比對同時涵蓋對外購買編號與資料庫 UUID，支援完整或部分文字，英文不區分大小寫。
- purchase_id 條件會與教練及會員名稱條件交集篩選，不改變資料庫內容或權限。

## v1.9.3 銷課搜尋無結果提示

- 銷課登錄的會員名稱與課程下拉搜尋若無符合項目，畫面顯示「查無此人及課程」。
- 此項調整僅修改前端提示文字，不影響 Supabase 查詢、課程資格或權限篩選。
- 「選擇課程」選項顯示會員名稱、課程名稱、成交教練、剩餘堂數及餘額；會員名稱來自目前已選取的會員資料。

## v1.9.5 銷課紀錄選擇顯示

- 資料管理「修改／刪除 → 銷課表」的選擇紀錄顯示順序為：purchase_id、會員名稱、教練、課程名稱、堂數。
- 課程名稱由 `purchase_balances.course_name` 依 `purchase_id` 關聯帶入；堂數為 `session_usages.session_seq`。

## v1.9.6 銷課紀錄銷課日期

- 資料管理「修改／刪除 → 銷課表」選擇紀錄新增 `session_usages.usage_date`。
- 選擇紀錄顯示順序為：purchase_id、會員名稱、教練、課程名稱、堂數、銷課日期。

## v1.9.7 銷課查詢完整性

- 資料管理「資料匯入／匯出 → 銷課查詢」依查詢日期與教練條件分批讀取 `session_usages`。
- 分批排序鍵為 `usage_date desc, created_at desc, id desc`，避免同日紀錄跨批時重複或遺漏。
- 會員名稱仍由 `session_usages.purchase_id` 關聯 `purchases.member_id` 及 `members.member_name`，不改變原有查詢權限與匯出欄位。

## v1.9.8 銷課表分頁順序

- 銷課表分頁由左至右為：銷課登錄、上課預約取消、教練查詢。
- 本版僅調整 Streamlit 分頁顯示順序，不變更資料表、欄位或權限。

## v1.9.9 動磁波時數

- 動磁波課程判斷條件：`purchases.course_name` 包含「動磁波」字樣。
- 一般「銷課時數」排除動磁波課程；「動磁波時數」僅計算符合條件的課程。
- 「總執行時數」與每月教練「時數總計」均為一般銷課時數＋動磁波時數＋其他原有營運時數，不重複計算。

## v1.9.4 修改／刪除銷課完整查詢

- 資料管理「修改／刪除」的銷課表不再只載入全系統最新 500 筆。
- `session_usages` 依每批 1,000 筆分頁讀取，排序鍵依序為 `usage_date DESC, created_at DESC, id DESC`，完成後才套用教練、會員名稱及 purchase_id 篩選。
- 此更新只修正查詢完整性，不修改既有銷課資料、扣課金額或權限。
