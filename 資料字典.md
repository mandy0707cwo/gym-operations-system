# 資料字典

| 資料表 | 用途 | 主鍵／重要關聯 |
|---|---|---|
| `profiles` | 使用者、顯示名稱、教練／主管角色 | `id` 對應 Supabase Auth user |
| `members` | 會員主檔 | `created_by` → profiles |
| `daily_operations` | 每日營運指標 | 日期 + 教練唯一；`coach_id` → profiles |
| `purchases` | 課程購買主檔 | `member_id` → members；`coach_id` → profiles |
| `purchase_payments` | 各期實際付款 | `purchase_id` → purchases；同購買期次唯一 |
| `session_usages` | 每次銷課與金額 | `purchase_id` → purchases；同購買堂次序號唯一 |
| `purchase_balances` | 即時計算使用／剩餘堂數及金額 | 資料庫 View，不另存重複餘額 |

## 主要欄位定義

- `classes_held`：實際完成上課堂數。
- `classes_cancelled`：排定但取消堂數；Dashboard 取消率分母為完成堂數 + 取消堂數。
- `trial_conversions`：體驗後成交人次，不得大於同筆體驗人次。
- `total_amount`：該次購買的成交總額；幣別由營運政策統一，本版畫面以 `$` 符號顯示。
- `installment_count`：購買約定總期數，1–3 期。
- `deducted_amount`：該堂認列的扣課金額；非會計收入認列憑證。

## v1.1.0 專案資料

| 資料表／檢視 | 用途 |
|---|---|
| `projects` | 非會員專案主檔、已儲值／未儲值類型及儲值金額 |
| `project_catalog` | 每個專案的操作項目、時數與價格 |
| `project_entries` | 專案每日執行明細、使用者、教練、數量、金額與備註 |
| `project_funding_balances` | 已儲值專案的儲值、已使用及剩餘金額 |
| `project_deposits` | 專案每次儲值、期初儲值及沖銷歷史 |

- `funding_type`：`stored` 為已儲值；`unfunded` 為未儲值／事後請款。
- `stored_date`：實際收到專案儲值款的日期；未儲值專案為空白。
- `stored_amount`：專案實際儲值總金額，不得低於已使用金額。
- `project_deposits.amount`：正數為儲值；負數為沖銷。累計加總應與專案儲值總額一致。

## v1.4.0 每月報表資料

- `operation_item_catalog.default_amount`：體驗項目或單堂銷售的預設未稅金額。
- `trial_items.amount`：體驗項目建立當下的未稅金額快照。
- `single_sales.amount`：單堂銷售建立當下的未稅金額快照。
- 專案及銷課原始金額視為含稅，月報未稅金額按含稅金額除以 1.05 後四捨五入至整數。

## v1.4.3 課程狀態

- `course_catalog.active`：`true` 為啟用，可供新購買選擇；`false` 為停用，僅保留歷史資料。

## v1.5.0 體驗項目內容

- `operation_item_catalog.item_name`：體驗項目名稱。
- `operation_item_catalog.detail_content`：該體驗項目預設帶入的內容。
- `trial_items.content`：建立紀錄當下的體驗項目快照。
- `trial_items.detail_content`：建立紀錄當下的內容快照。
- `line_amount`：每筆專案操作的實際金額快照，避免單價除算造成差額。
## v1.5.1 體驗項目唯一性

- 體驗項目以 `item_type + item_name + detail_content` 判斷是否重複。
- 同一個體驗項目名稱可以建立多個不同內容。
- 非體驗類型仍以 `item_type + item_name` 判斷是否重複。
## v1.6.0 體驗項目狀態與課程購買修改

- `operation_item_catalog.active`：體驗項目是否可供新紀錄選用；停用不刪除歷史資料。
- 課程購買修改可更新 `purchases.purchase_kind` 與 `purchases.coach_id`。
- 購課紀錄搜尋顯示 `purchase_date`、會員名稱、課程名稱、`total_sessions` 與指導教練。
## v1.6.1 資料管理介面

- 課程名稱、體驗項目及單堂銷售的新增、刪除與修改功能分頁呈現。
- 修改課程目錄不回寫 `purchases` 的歷史成交快照。
- 修改營運項目目錄不回寫 `trial_items` 或 `single_sales` 的歷史紀錄。
## v1.6.2 專案操作項目管理

- 專案操作項目以新增、刪除及修改三個分頁呈現。
- 修改 `project_catalog` 的項目、時數或價格，不回寫 `project_entries` 的歷史單據快照。
## v1.6.3 會員財務報表欄位

- 「實際預收金額」：查詢期間內實際收到的未分期或分期款項。
- 「實際預收金額總計」：查詢結果中實際收款的合計。
- 欄位更名不改變原有含稅／未稅換算及篩選邏輯。
## v1.6.4 會員銷課總表

- `購買_ID`：銷課紀錄所屬的 `purchases.id` 完整識別碼。
- `堂數`：對應 `session_usages.session_seq`，表示該購買紀錄目前銷課至第幾堂；堂次序號不加總。
- `銷課金額`：依使用者選擇顯示含稅或未稅金額。
